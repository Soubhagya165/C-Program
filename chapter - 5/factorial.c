#include<stdio.h>
int factorial(int x)
{
    {
        fact = fact*i;
    }
    return fact;
}
int main () {
    int n,z;
    printf("enter a number : ");
    scanf("%d%d",&n,&z);
    int fact = factorial(n);
    printf("the factorial is %d\n",fact);
    return 0;
}